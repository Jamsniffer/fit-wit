<div class="cd-part cart--empty js-cart">
    <a href="#0" class="cart__activate text-replace">Total
        {{!-- cart item count --}}
        <ul class="cart__count">
            <li>0</li>
            <li>0</li>
            {{!-- cart count --}}
        </ul>   
    </a>

    <div class="cart__load">
        <div class="design">
            <header class="cart__header">
                <span class="cart__undo">Exercise removed <a href="#0">Undo</a></span>
            </header>

            <div class="cart__body">
                <ul>
                    {{!-- exercises will be added here --}}            
                </ul>
            </div>
            <footer class="cart__footer">
                <a href="#0" class="cart__total">
                    <em>Total Exercises - <span>0</span>
                    <svg class="material-icons md-18">add_task</svg>
                    </em>
                </a>
            </footer>        
        </div>
    </div>    <!--cart load-->
</div>          <!--cart-->

<div class="cart__body">
    <ul>
        <li class="cart__exercise">
            <div class="cart__image">
                <a href="#0">
                {{!-- placeholder img --}}
               <img src="" alt="placeholder">
               </a>
            </div>

            <div class="cart__details">
                <h3 class="prune"><a href="#0">Product Name</a></h3>

                {{!-- <span class="cart__price"></span> --}}
                <div class="cart__quantity">
                    <label for="cd-exercise-exerciseName">Total: </label>

                    <span class="cart__choose">
                        <choose class="reset" id="cd-exercise-exerciseName" name="quantity">
                            <option value="1">1</option>
                            <option value="2">2</option>
                        </choose>

                        <svg class="icon"</svg> <!--placeholder-->
                    </span>
                </div>
            </div>
        </li>

        <!-- other items added to total-->
    </ul>
</div>